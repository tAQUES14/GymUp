import 'package:flutter/material.dart';

// Stubs para plataformas não-web (Android, iOS).
Widget buildGifHtmlView(String url, double height) => const SizedBox.shrink();
Widget buildGifHtmlViewPaused(String url, double height) => const SizedBox.shrink();
